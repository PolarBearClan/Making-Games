using UnityEngine;

public class NoiseObstaclesManager : MonoBehaviour
{
    public delegate void NoiseMade();
    public NoiseMade OnNoiseMade;
        
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
